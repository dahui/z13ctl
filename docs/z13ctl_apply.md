## z13ctl apply

Apply a lighting effect

```
z13ctl apply [flags]
```

### Examples

```
  z13ctl apply --color cyan --brightness high
  z13ctl apply --color 00FF88 --mode rainbow --speed slow
  z13ctl apply --mode breathe --color hotpink --color2 blue
  z13ctl apply --list-colors
```

### Options

```
      --brightness string   Brightness level: off|low|medium|high (default "high")
      --color string        Primary color: hex (RRGGBB) or name (e.g. red, cyan, hotpink). Use --list-colors for all names. (default "FF0000")
      --color2 string       Secondary color for breathe mode: hex (RRGGBB) or name. Use --list-colors for all names. (default "000000")
  -h, --help                help for apply
      --list-colors         List all supported color names with swatches
      --mode string         Lighting mode: static|breathe|cycle|rainbow|star|rain|strobe|comet|flash (default "static")
      --speed string        Animation speed: slow|normal|fast (default "normal")
```

### Options inherited from parent commands

```
      --device string   Target device: keyboard, lightbar, or a hidraw path (default: all)
      --dry-run         Print packets without sending to device
```

### SEE ALSO

* [z13ctl](z13ctl.md)	 - ROG Flow Z13 RGB lighting control

###### Auto generated by spf13/cobra on 20-Feb-2026
