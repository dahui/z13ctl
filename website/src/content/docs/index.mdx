---
title: Voltaire
description: RGB lighting, fan curves, TDP, and system control for the 2025 ASUS ROG Flow Z13 on Linux.
template: splash
hero:
  tagline: >-
    RGB lighting, performance profiles, battery limit, fan curves, TDP control,
    CPU undervolting, and display settings for the 2025 ASUS ROG Flow Z13 on
    Linux. Formerly z13ctl and z13gui.
  actions:
    - text: Install
      link: /voltaire/installation/
      icon: right-arrow
      variant: primary
    - text: Migrating from z13ctl?
      link: /voltaire/migrating-from-z13ctl/
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## What Voltaire does

<CardGrid>
	<Card title="RGB lighting" icon="star">
		Color, mode, speed, and brightness on the keyboard backlight and edge
		lightbar — independently or together.
	</Card>
	<Card title="Performance profiles" icon="rocket">
		Switch between `quiet`, `balanced`, and `performance`, or build named
		custom profiles with their own fan curve, TDP, and undervolt.
	</Card>
	<Card title="Fan curves" icon="setting">
		Custom 8-point fan curves via the asus-wmi hwmon interface, defended
		against the kernel silently dropping them.
	</Card>
	<Card title="TDP control" icon="warning">
		CPU/GPU power limits from 5 to 93 W, with an automatic fan floor above
		75 W sustained — 50% PWM rising to 100% at 80 °C.
	</Card>
	<Card title="CPU undervolting" icon="download">
		Lower voltage via AMD Curve Optimizer for better temperatures and power
		draw at the same performance (requires the `ryzen_smu` kernel module).
	</Card>
	<Card title="Touch-first GUI" icon="laptop">
		`voltaire-gui` is a GTK4 overlay drawer covering every feature — on the
		desktop and in Steam Gaming Mode, with full gamepad navigation.
	</Card>
</CardGrid>

Also: battery charge limit (40–100%), POST boot sound, panel overdrive, and any
other firmware toggle the device data describes.

All features work without root after a one-time `setup` step, and all persist
across reboots when the daemon is running. The daemon re-applies keyboard
lighting when the detachable keyboard is reattached, and puts your fan curve
back when the kernel discards it — which it does on every system power profile
change (see [Daemon](/voltaire/reference/daemon/#custom-fan-curve-reconciliation)).

:::tip[New to Linux? Use the GUI]
Most users — especially those newer to Linux — should install
**voltaire-gui**, the touch-friendly GTK4 overlay that controls every feature
(lighting, fan curves, TDP, undervolt, profiles, battery limit) through the
daemon — no command line required. The raw `voltaire` CLI is great for
scripting and advanced tuning, but if you're not a Linux veteran you'll have a
far smoother experience with the GUI. Both install together — see
[Installation](/voltaire/installation/).
:::

## Background

Linux support for RGB control on the 2025 ASUS ROG Flow Z13 is limited.
OpenRGB does not support this model. asusctl does not work with it.
rogauracore only controls the keyboard backlight. HHD works on Bazzite kernels
but requires out-of-tree patches that reduce performance on CachyOS.

Voltaire implements the Aura HID protocol directly against the Linux `hidraw`
interface — no kernel patches, no external daemons. The protocol was
reverse-engineered from [g-helper](https://github.com/seerge/g-helper) (MIT).
A full protocol specification is available in the
[Protocol Reference](/voltaire/reference/protocol/).

Voltaire is the continuation of **z13ctl** and **z13gui**, merged into one
project at 2.0. Existing installs upgrade in place — state, config, and the
daemon socket all carry over. See
[Migrating from z13ctl](/voltaire/migrating-from-z13ctl/).

## Requirements

- Linux kernel with `hidraw` support
- 2025 ASUS ROG Flow Z13 (USB IDs `0b05:18c6` and `0b05:1a30`)
- Read/write access to the relevant `hidraw` and sysfs files — either run as
  root, or use [`voltaire setup`](/voltaire/reference/commands/#setup) to
  install udev rules granting access to a group

## Next steps

Head to the [Installation](/voltaire/installation/) guide to get Voltaire on
your system, then follow the [Quick Start](/voltaire/quick-start/) to configure
lighting, fan curves, and performance profiles. For the full CLI reference, see
[Commands](/voltaire/reference/commands/).
